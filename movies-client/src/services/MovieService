import http from "../helpers/http.js";

class MovieService {
  list(query) {
    const url = query ? `/Movies?title=${query}` : "/Movies";
    return http.get(url);
  }

  getById(id) {
    return http.get(`/Movies/${id}`);
  }
}

export default new MovieService();
